
create table source_user_event (
    event_time text,
    user_id text,
    event_type text,
    platform text,
    country text,
    region text,
    device_id text,
    initial_referring_domain text
);
